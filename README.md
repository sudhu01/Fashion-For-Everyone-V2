# Fashion For Everyone V2.0
## Setup Guide:

1. Create a new python virtual environment in the project root -> **python -m venv .venv**

2. Activate the virtual environment -> **./venv/Scripts/activate**

3. Install all dependencies -> **pip install -r requirements.txt**

4. Run the myntra scraper -> **cd dataset-scraper; py myntra.py**